---
paths:
  - "**/*"
---

When adding, removing, or changing an environment variable; adding a new NiFi
Processor, Controller Service, or Reporting Task; or adding a new tool or child
module, apply the `update-project-documentation` skill to keep `README.md` and
the `docs/` guides in sync.
